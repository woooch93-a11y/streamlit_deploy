# 로컬 개발용: 이 파일을 복사해 `secrets.toml`로 저장한 뒤 키를 넣으세요.
# `secrets.toml`은 Git에 올리지 마세요 (.gitignore에 포함됨).
#
# Streamlit Community Cloud에서는 대시보드 → 앱 → Settings → Secrets 에서 동일 키를 설정합니다.

OPENAI_API_KEY = "sk-..."

# 선택: 사이드바 기본 모델 문자열 (앱에서 수정 가능)
# OPENAI_MODEL = "gpt-4o-mini"
